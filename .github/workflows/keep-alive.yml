name: Keep API and DB warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /health
        run: curl -sf --max-time 180 --retry 3 --retry-all-errors https://piki-dada-api-xgen.onrender.com/health
